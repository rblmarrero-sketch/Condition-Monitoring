/* Undercarriage wear reference — GENERATED by docs/build-wear-data.py.
   Do not hand-edit; edit docs/wear-limits.json or the build script and re-run.

   Every new/condemn pair here came out of a cell in the EX or DZ workbook.
   Where the workbook's own figures cannot be right, the point is marked `x`
   and the app records the millimetres without scoring them:

     x:"borrowed"  the model's figures belong to a different model
     x:"rival"     two tabs of the same model disagree

   THE REFERENCE IS NOT THE LAST WORD. A baseline measured on this unit when the
   undercarriage was new supersedes the catalogue figure from its date forward —
   see WEAR.refFor(). Old readings keep being judged against whatever reference
   was in force when they were taken, so history never silently changes.

   16 models, 123 reference pairs.
*/
window.WEAR = {"built":"2026-08-26","points":[{"code":"IDLER","en":"Idler tread","ru":"Направляющее колесо","pos":"WHEEL","tool":"straightedge + depth rule"},{"code":"CARRIER","en":"Carrier roller","ru":"Поддерживающий каток","pos":"WHEEL","tool":"caliper"},{"code":"ROLLER","en":"Track roller","ru":"Опорный каток","pos":"ROLLERS","tool":"caliper"},{"code":"SPROCKET","en":"Sprocket","ru":"Звёздочка","pos":"SIDE","tool":"tape across the teeth"},{"code":"LINKH","en":"Link height","ru":"Высота звена","pos":"SIDE","tool":"caliper"},{"code":"BUSH","en":"Bushing OD","ru":"Втулка","pos":"SIDE","tool":"caliper"},{"code":"PITCH4","en":"Link pitch — 4 link","ru":"Шаг звеньев, 4 звена","pos":"SIDE","tool":"tape"},{"code":"PITCH1","en":"Link pitch — 1 link","ru":"Шаг звеньев, 1 звено","pos":"SIDE","tool":"tape"},{"code":"GROUSER","en":"Grouser height","ru":"Высота грунтозацепа","pos":"SIDE","tool":"rule"}],"models":{"CAT 336":{"pts":{"BUSH":{"n":70,"c":65.8,"d":"shrink"},"CARRIER":{"n":160,"c":132.5,"d":"shrink"},"GROUSER":{"n":71,"c":38,"d":"shrink"},"IDLER":{"n":20,"c":29,"d":"grow"},"LINKH":{"n":124,"c":115,"d":"shrink"},"PITCH1":{"n":216,"c":219,"d":"grow"},"PITCH4":{"n":865,"c":877.5,"d":"grow"},"ROLLER":{"n":180,"c":162.5,"d":"shrink"}},"units":["EX013"],"rollers":8,"frame":"oval","missing":["SPROCKET"]},"CAT D9R":{"pts":{"BUSH":{"n":84.8,"c":77.2,"d":"shrink"},"CARRIER":{"n":250,"c":189,"d":"shrink"},"GROUSER":{"n":84,"c":38,"d":"shrink"},"IDLER":{"n":20,"c":36,"d":"grow"},"LINKH":{"n":166,"c":149.5,"d":"shrink"},"PITCH1":{"n":240,"c":246.4,"d":"grow"},"PITCH4":{"n":960,"c":985.6,"d":"grow"},"ROLLER":{"n":260,"c":186,"d":"shrink"},"SPROCKET":{"n":266.5,"c":240,"d":"shrink"}},"units":["DZ010"],"rollers":8,"frame":"highdrive"},"Hitachi ZX 1200":{"pts":{"BUSH":{"n":98,"c":90,"d":"shrink"},"CARRIER":{"n":210,"c":202,"d":"shrink"},"GROUSER":{"n":50,"c":35,"d":"shrink"},"IDLER":{"n":23,"c":29,"d":"grow"},"LINKH":{"n":116,"c":109,"d":"shrink"},"PITCH1":{"n":280,"c":286,"d":"grow"},"PITCH4":{"n":1122.3,"c":1145.2,"d":"grow"},"ROLLER":{"n":290,"c":270,"d":"shrink"}},"units":["EX001","EX002"],"rollers":8,"frame":"oval","missing":["SPROCKET"]},"Hitachi ZX 280":{"pts":{"BUSH":{"n":66.91,"c":61.9,"d":"shrink"},"CARRIER":{"n":150,"c":140,"d":"shrink"},"GROUSER":{"n":30,"c":21,"d":"shrink"},"IDLER":{"n":22.5,"c":29.5,"d":"grow"},"LINKH":{"n":116,"c":109,"d":"shrink"},"PITCH1":{"n":216.48,"c":223.25,"d":"grow"},"PITCH4":{"n":865.92,"c":983,"d":"grow"},"ROLLER":{"n":180,"c":162,"d":"shrink"}},"units":["EX016"],"rollers":8,"frame":"oval","missing":["SPROCKET"]},"Hitachi ZX 330":{"pts":{"BUSH":{"n":66.91,"c":61.9,"d":"shrink"},"CARRIER":{"n":150,"c":140,"d":"shrink"},"GROUSER":{"n":30,"c":21,"d":"shrink"},"IDLER":{"n":22.5,"c":29.5,"d":"grow"},"LINKH":{"n":116,"c":109,"d":"shrink"},"PITCH1":{"n":250,"c":255,"d":"grow","rivals":[{"n":216.48,"c":223.25}],"x":"rival"},"PITCH4":{"n":865.92,"c":893,"d":"grow"},"ROLLER":{"n":180,"c":162,"d":"shrink"}},"units":["EX007","EX008","EX019","EX020"],"rollers":8,"frame":"oval","disputed":{"PITCH1":"rival"},"note":"Two different link pitches under one model name: 250 mm on EX007/EX019 and 216 mm on EX008/EX020. Two undercarriage builds, one label.","missing":["SPROCKET"]},"Hitachi ZX 470LC":{"pts":{"BUSH":{"n":71.35,"c":66.4,"d":"shrink"},"CARRIER":{"n":170,"c":160,"d":"shrink"},"GROUSER":{"n":36,"c":24,"d":"shrink"},"IDLER":{"n":22.5,"c":19.2,"d":"shrink"},"LINKH":{"n":129,"c":122,"d":"shrink"},"PITCH1":{"n":215,"c":220.25,"d":"grow"},"PITCH4":{"n":863.3,"c":905.3,"d":"grow"},"ROLLER":{"n":200,"c":182,"d":"shrink"}},"units":["EX004","EX005"],"rollers":8,"frame":"oval","missing":["SPROCKET"]},"Komatsu 275A":{"pts":{"BUSH":{"n":90,"c":82,"d":"shrink"},"CARRIER":{"n":200,"c":175,"d":"shrink"},"GROUSER":{"n":88,"c":30,"d":"shrink"},"IDLER":{"n":21,"c":33.5,"d":"grow"},"LINKH":{"n":166,"c":148,"d":"shrink"},"PITCH1":{"n":260.6,"c":263.6,"d":"grow"},"PITCH4":{"n":1042.4,"c":1054.4,"d":"grow"},"ROLLER":{"n":255,"c":185,"d":"shrink"}},"units":["DZ003"],"rollers":8,"frame":"oval","missing":["SPROCKET"]},"Komatsu D155A":{"pts":{"BUSH":{"n":81,"c":75,"d":"shrink"},"CARRIER":{"n":190,"c":171,"d":"shrink"},"GROUSER":{"n":80,"c":30,"d":"shrink"},"IDLER":{"n":21,"c":33.5,"d":"grow"},"LINKH":{"n":144,"c":128,"d":"shrink"},"PITCH1":{"n":228.85,"c":233.85,"d":"grow"},"PITCH4":{"n":915.4,"c":935.4,"d":"grow"},"ROLLER":{"n":250,"c":210,"d":"shrink"}},"units":["DZ001"],"rollers":8,"frame":"oval","missing":["SPROCKET"]},"Komatsu D375A":{"pts":{"BUSH":{"n":98.5,"c":92.5,"d":"shrink"},"CARRIER":{"n":210,"c":185,"d":"shrink"},"GROUSER":{"n":93,"c":30,"d":"shrink"},"IDLER":{"n":23.5,"c":36,"d":"grow"},"LINKH":{"n":181,"c":163,"d":"shrink"},"PITCH1":{"n":280.3,"c":283.3,"d":"grow"},"PITCH4":{"n":1121.2,"c":1133.2,"d":"grow"},"ROLLER":{"n":280,"c":205,"d":"shrink"}},"units":["DZ005","DZ006","DZ008","DZ009"],"rollers":8,"frame":"oval","missing":["SPROCKET"]},"Komatsu PC800SE":{"pts":{"BUSH":{"n":87,"c":82.9,"d":"shrink"},"CARRIER":{"n":175,"c":155,"d":"shrink"},"GROUSER":{"n":50,"c":25,"d":"shrink"},"IDLER":{"n":22,"c":28.5,"d":"grow"},"LINKH":{"n":156,"c":143,"d":"shrink"},"PITCH1":{"n":260,"c":263.6,"d":"grow"},"PITCH4":{"n":1042,"c":1054.4,"d":"grow"},"ROLLER":{"n":255,"c":243,"d":"shrink"}},"units":["EX009","EX010"],"rollers":8,"frame":"oval","missing":["SPROCKET"]},"Shantui SD32":{"pts":{"BUSH":{"n":79,"c":74,"d":"shrink"},"CARRIER":{"n":190,"c":171,"d":"shrink"},"GROUSER":{"n":80,"c":25,"d":"shrink"},"IDLER":{"n":25,"c":30,"d":"grow"},"LINKH":{"n":138,"c":125,"d":"shrink"},"PITCH1":{"n":228.6,"c":233.85,"d":"grow"},"PITCH4":{"n":914.4,"c":895,"d":"shrink"},"ROLLER":{"n":255,"c":229,"d":"shrink"}},"units":["DZ014","DZ016"],"rollers":8,"frame":"oval","missing":["SPROCKET"]},"Shantui SD34":{"pts":{"BUSH":{"n":79,"c":74,"d":"shrink"},"CARRIER":{"n":190,"c":171,"d":"shrink"},"GROUSER":{"n":80,"c":25,"d":"shrink"},"IDLER":{"n":25,"c":30,"d":"grow"},"LINKH":{"n":138,"c":125,"d":"shrink"},"PITCH1":{"n":228.6,"c":233.85,"d":"grow"},"PITCH4":{"n":914.4,"c":895,"d":"shrink"},"ROLLER":{"n":255,"c":229,"d":"shrink"}},"units":["DZ012","DZ020"],"rollers":8,"frame":"oval","missing":["SPROCKET"]},"Shantui SD60":{"pts":{"BUSH":{"n":98,"c":97.65,"d":"shrink"},"CARRIER":{"n":210,"c":185,"d":"shrink"},"GROUSER":{"n":93,"c":30,"d":"shrink"},"IDLER":{"n":23.5,"c":36,"d":"grow"},"LINKH":{"n":181,"c":163,"d":"shrink"},"PITCH1":{"n":280,"c":283,"d":"grow"},"PITCH4":{"n":1120,"c":1132,"d":"grow"},"ROLLER":{"n":270,"c":200,"d":"shrink"}},"units":["DZ019","DZ021","DZ022"],"rollers":8,"frame":"oval","missing":["SPROCKET"]},"Shantui SD90":{"pts":{"BUSH":{"n":79,"c":74,"d":"shrink","x":"borrowed"},"CARRIER":{"n":190,"c":171,"d":"shrink","x":"borrowed"},"GROUSER":{"n":80,"c":25,"d":"shrink","x":"borrowed"},"IDLER":{"n":25,"c":30,"d":"grow","x":"borrowed"}},"units":["DZ017","DZ018"],"rollers":8,"frame":"oval","disputed":{"BUSH":"borrowed","CARRIER":"borrowed","GROUSER":"borrowed","IDLER":"borrowed"},"note":"Carries the SD32 figures. Bushings on both SD90s measure 114-116 mm against a 79 mm \"new\" — impossible. Set a baseline instead.","missing":["ROLLER","SPROCKET","LINKH","PITCH4","PITCH1"]},"LiuGong 970E":{"pts":{"CARRIER":{"n":200,"c":106,"d":"shrink"},"GROUSER":{"n":50,"c":40,"d":"shrink"},"IDLER":{"n":22.5,"c":28.5,"d":"grow"},"LINKH":{"n":156,"c":147,"d":"shrink"},"PITCH1":{"n":260.35,"c":263.35,"d":"grow"},"PITCH4":{"n":1041.4,"c":1053.4,"d":"grow"},"ROLLER":{"n":180,"c":168,"d":"shrink"}},"units":["EX024","EX025"],"rollers":8,"frame":"oval","missing":["SPROCKET","BUSH"]},"LiuGong 990F":{"pts":{"CARRIER":{"n":200,"c":192,"d":"shrink"},"GROUSER":{"n":50,"c":40,"d":"shrink"},"IDLER":{"n":22,"c":28.5,"d":"grow"},"LINKH":{"n":156,"c":147,"d":"shrink"},"PITCH1":{"n":260.35,"c":263.35,"d":"grow"},"PITCH4":{"n":1041.4,"c":1053.4,"d":"grow"},"ROLLER":{"n":270,"c":258,"d":"shrink"}},"units":["EX021","EX022"],"rollers":8,"frame":"oval","missing":["SPROCKET","BUSH"]}},"map":{"CATERPILLAR 336-07":{"m":"CAT 336","via":"exact"},"CATERPILLAR D9R":{"m":"CAT D9R","via":"exact"},"HITACHI EX1200-6BH":{"m":"Hitachi ZX 1200","via":"exact"},"HITACHI EX1200-7BH":{"m":"Hitachi ZX 1200","via":"inherited"},"HITACHI ZX280-5G":{"m":"Hitachi ZX 280","via":"exact"},"HITACHI ZX330-5G RB":{"m":"Hitachi ZX 330","via":"exact"},"HITACHI ZX470LC-5G":{"m":"Hitachi ZX 470LC","via":"exact"},"HITACHI ZX470LCR-5G":{"m":"Hitachi ZX 470LC","via":"inherited"},"KOMATSU D155A.5":{"m":"Komatsu D155A","via":"exact"},"KOMATSU D275.5D":{"m":"Komatsu 275A","via":"exact"},"KOMATSU D375A.6":{"m":"Komatsu D375A","via":"exact"},"KOMATSU PC800-8E0 (SE)":{"m":"Komatsu PC800SE","via":"exact"},"LiuGong CLG970E":{"m":"LiuGong 970E","via":"exact"},"LiuGong CLG990FHD":{"m":"LiuGong 990F","via":"exact"},"SHANTUI SD32":{"m":"Shantui SD32","via":"exact"},"SHANTUI SD34-B3":{"m":"Shantui SD34","via":"exact"},"SHANTUI SD60-C5":{"m":"Shantui SD60","via":"exact"},"SHANTUI SD90-C5":{"m":"Shantui SD90","via":"exact"},"KOMATSU PC2000-8 BH":{"m":null,"via":"none"}},"exclude":{"HITACHI ZX210W-5A":"wheeled"},"reasons":[{"code":"GUARD","en":"Behind the guard","ru":"Кожух"},{"code":"NOFIT","en":"Not fitted on this machine","ru":"Не установлен"},{"code":"PACKED","en":"Packed with mud or ice","ru":"Забито грязью/льдом"},{"code":"ACCESS","en":"Cannot reach it","ru":"Нет доступа"},{"code":"PARK","en":"Machine parked wrong","ru":"Машина стоит неудобно"},{"code":"TOOL","en":"No tool for it","ru":"Нет инструмента"},{"code":"OTHER","en":"Other — see comment","ru":"Другое — см. комментарий"}],"rollersDefault":8};

/* ---- reference resolution -------------------------------------------------
   catalogue  the model figures above
   baseline   measured on THIS unit when the part was new, and dated
   override   an engineer's correction, with a reason

   Baselines and overrides arrive as records, exactly like inspections, so they
   sync, they can be voided, and they never rewrite what came before. */
(function (W) {
  W.baselines = [];                       // [{unit,point,pos,n,c,from,by,why}]

  W.setBaselines = function (rows) { W.baselines = rows || []; };

  W.modelFor = function (regModel) {
    var e = W.map[regModel];
    return e && e.m ? W.models[e.m] : null;
  };
  W.eligible = function (regModel) { return !W.exclude[regModel]; };

  /* The reference in force for one unit, one point, one position, on one date. */
  W.refFor = function (unit, regModel, point, pos, atISO) {
    var at = atISO || '9999-12-31', best = null;
    for (var i = 0; i < W.baselines.length; i++) {
      var b = W.baselines[i];
      if (b.unit !== unit || b.point !== point) continue;
      if (b.pos && pos && b.pos !== pos) continue;      // a blank pos covers the whole point
      if (b.from > at) continue;
      if (!best || b.from > best.from) best = b;
    }
    var cat = null, m = W.modelFor(regModel);
    if (m && m.pts[point]) cat = m.pts[point];
    if (best) {
      /* A baseline replaces "new" only, and it inherits the catalogue's condemn
         where there is one worth inheriting. A FLAGGED one is not: the SD90
         carries the SD32's whole row, so taking its condemn while replacing its
         "new" would score a real measurement against a borrowed limit and print
         "vs the baseline set" over it, which reads as though somebody checked.
         Either the baseline carries its own condemn or there is still nothing
         to score against, and the app says so. */
      var inherit = (cat && !cat.x) ? cat.c : null;
      var c = (best.c != null ? best.c : inherit);
      return { n: best.n, c: c,
               d: cat ? cat.d : (best.c > best.n ? 'grow' : 'shrink'),
               src: best.why === 'override' ? 'override' : 'baseline',
               from: best.from, by: best.by,
               x: (c == null) ? 'nolimit' : null };
    }
    if (!cat) return null;
    return { n: cat.n, c: cat.c, d: cat.d, src: 'catalogue', x: cat.x || null,
             rivals: cat.rivals || null };
  };

  /* Percent of the way from new to condemn. Null whenever it would be a lie. */
  W.wear = function (ref, mm) {
    if (!ref || mm == null || ref.x) return null;
    if (ref.n == null || ref.c == null) return null;
    var span = ref.c - ref.n;
    if (!span) return null;
    return ((mm - ref.n) / span) * 100;
  };

  /* Does the reading disagree with the reference badly enough to ask again?
     Never a block: the SD90s prove the reference can be the thing that is wrong. */
  W.implausible = function (ref, mm) {
    if (!ref || mm == null || ref.n == null || ref.c == null) return null;
    var tol = Math.max(1, Math.abs(ref.n) * 0.02);
    if (ref.d === 'shrink' && mm > ref.n + tol) return 'over-new';
    if (ref.d === 'grow' && mm < ref.n - tol) return 'under-new';
    var p = W.wear(ref, mm);
    if (p != null && p > 130) return 'past-condemn';
    return null;
  };

  /* ---- how long has it got -------------------------------------------------

     Percent worn says where a part is. It does not say when to order the
     replacement, and that is the number planning actually needs. Two dated
     readings and a condemn limit give it, and — this is the useful part — the
     "new" figure is not required at all. A point whose catalogue figures are
     missing, borrowed or disputed can still be forecast the moment somebody
     supplies a condemn limit.

     What it will not do is guess. It returns a reason instead of a number
     whenever the honest answer is "not yet":

       few       one reading is a dot, not a line
       soon      the interval is too short for the caliper to out-measure itself
       flat      no measurable movement — a rate of zero forecasts forever
       away      moving away from the limit, which is a data problem, not a life
       past      already at or past condemn; the band has said so

     Hours are preferred over days because a machine parked for a month has not
     worn, and SMU is what parts are ordered against. Calendar is the fallback
     when the hour meter was not written down, converted at a stated assumption
     so nobody mistakes it for a measured figure. */
  W.MIN_HOURS = 100;          // below this the caliper's own error dominates
  W.MIN_DAYS  = 14;
  /* Only used when the hour meter was not written down. It said 12 — one
     shift — and this fleet runs two, so every calendar-based forecast was
     reporting a machine as wearing at half the rate it does. One number, in
     due.js, shared with the inspection schedule: a forecast that disagrees with
     the interval it is scheduled against is two answers to one question. */
  W.HOURS_PER_DAY = (window.DUE && window.DUE.HOURS_PER_DAY) || 20;

  /* series: [{mm, at, smu}] oldest first, INCLUDING the reading being taken.
     `at` is an ISO date, `smu` the hour meter if it was recorded. */
  W.forecast = function (ref, series) {
    if (!ref || ref.c == null || !series || series.length < 2) return { why: 'few' };
    var pts = series.filter(function (r) { return r && r.mm != null; });
    if (pts.length < 2) return { why: 'few' };

    /* One x axis for the whole series, in hours. SMU if every point has it and
       it advances; otherwise calendar, flagged so the caller can say so. */
    var useSmu = pts.every(function (r) { return r.smu != null && r.smu !== '' && isFinite(Number(r.smu)); });
    if (useSmu) {
      var s0 = Number(pts[0].smu);
      for (var i = 1; i < pts.length; i++) if (Number(pts[i].smu) < s0) { useSmu = false; break; }
    }
    var x = pts.map(function (r) {
      if (useSmu) return Number(r.smu) - Number(pts[0].smu);
      return (Date.parse(r.at + 'T00:00:00Z') - Date.parse(pts[0].at + 'T00:00:00Z'))
             / 86400000 * W.HOURS_PER_DAY;
    });
    var span = x[x.length - 1] - x[0];
    if (!isFinite(span) || span <= 0) return { why: 'soon' };
    if (useSmu ? span < W.MIN_HOURS : span < W.MIN_DAYS * W.HOURS_PER_DAY)
      return { why: 'soon', basis: useSmu ? 'smu' : 'days' };

    /* Least squares over three or more points, because a caliper on a cold
       roller is worth about a millimetre and two points make that millimetre
       the whole trend. Two points get the plain slope, which is all there is. */
    var y = pts.map(function (r) { return Number(r.mm); }), slope;
    if (pts.length >= 3) {
      var n = pts.length, sx = 0, sy = 0, sxx = 0, sxy = 0;
      for (var j = 0; j < n; j++) { sx += x[j]; sy += y[j]; sxx += x[j] * x[j]; sxy += x[j] * y[j]; }
      var den = n * sxx - sx * sx;
      if (!den) return { why: 'soon' };
      slope = (n * sxy - sx * sy) / den;                 // mm per hour, signed
    } else {
      slope = (y[1] - y[0]) / (x[1] - x[0]);
    }

    var now = y[y.length - 1];
    var down = ref.d !== 'grow';                          // most points wear down
    var rate = down ? -slope : slope;                     // mm/h toward the limit
    var left = down ? (now - ref.c) : (ref.c - now);      // mm still to go

    if (left <= 0) return { why: 'past', basis: useSmu ? 'smu' : 'days' };
    /* A rate under a hundredth of a millimetre per thousand hours is noise
       wearing a decimal point. Treat it as no movement rather than 4,000,000 h. */
    if (rate <= 0.00001) return { why: rate < -0.00001 ? 'away' : 'flat',
                                  basis: useSmu ? 'smu' : 'days' };

    return { hours: left / rate,
             rate: rate * 1000,                           // mm per 1,000 hours
             left: left,
             basis: useSmu ? 'smu' : 'days',
             pts: pts.length,
             fit: pts.length >= 3 ? 'ls' : 'two' };
  };

  /* Every position on one machine, in walk order. */
  W.walk = function (regModel) {
    var m = W.modelFor(regModel), out = [];
    var n = (m && m.rollers) || W.rollersDefault;
    W.points.forEach(function (p) {
      var list = p.pos === 'SIDE' ? ['L', 'R']
               : p.pos === 'WHEEL' ? ['L-OUT', 'L-IN', 'R-OUT', 'R-IN']
               : [];
      if (p.pos === 'ROLLERS') {
        for (var s = 0; s < 2; s++)
          for (var i = 1; i <= n; i++) list.push((s ? 'R' : 'L') + i);
      }
      list.forEach(function (pos) { out.push({ point: p.code, pos: pos, k: p.code + '.' + pos }); });
    });
    return out;
  };
})(window.WEAR);
