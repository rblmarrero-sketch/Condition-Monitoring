{
  "_purpose": "Bookmark for the hourly diagnostic-trace review Routine. Holds the `updated` timestamp (ms epoch, from the _meta/diag/ folder listing on the live backend) of the newest trace file already reviewed. Each hourly check reads this, looks only at traces newer than it, analyzes anything actionable, and advances it — so the review never re-reads the same trace twice and never silently skips one. Updated by that Routine, not by hand.",
  "lastReviewedUpdated": 1789877021546,
  "lastReviewedName": "D1ZMK6_2026-09-20T04-03-40.json",
  "lastCheckedAt": "2026-09-20T04:40:24Z",
  "watching": [
    {
      "note": "FOUR occurrences now, four different devices, all with the same benign outcome: DKB8FF 2026-09-19T22:32 (Id:2), D4W4FT 2026-09-20T02:09 (Id:1), DO3JLV 2026-09-20T03:03 (Id:3), D8B32O 2026-09-20T03:16 (Id:3). Same shape every time — ~10-30 identical unhandled-rejection 'Object Not Found Matching Id:N, MethodName:update, ParamCount:4' in under 4s, immediately followed by a clean run (pending:0, run-end err:''). No data-loss evidence in any of the four. This is a WebKit-native message-bridge artifact (a callback routed to an already-torn-down page context) — there is no 'update' RPC this app's own JS calls, so it originates below the JS layer and is not fixable from here. Downgrading from 'watching for a repeat' to 'known benign, recurring' — still logging each new occurrence in case one ever arrives WITHOUT a clean sync after it, which would change this.",
      "status": "known-benign-recurring"
    },
    {
      "note": "ESCALATING, not just persisting. As of 2026-09-20T04:03 D1ZMK6 has now failed to read EVERY photograph on FIVE separate rounds across THREE round types — TK109 MP (11 of 12), DZ006/DZ007/DZ008 UC (3-4 each), and a NEWLY CAPTURED TK109 INSP round (4 of 4, its overview and all three CH.UC photos) — spanning at least five sync attempts from 01:27 to 04:03. A brand-new round failing 100% the same way as four-hour-old ones rules out 'this one file went stale' and points at something wrong with THIS PHONE's storage generally, not a handful of unlucky blobs. Consistent with that: writeback-fail ('save failed — storage may be full') has fired on every single record on every attempt, despite storage sitting at 16 MB of a 41 GB quota (0%) — the literal words are the dbPut onerror/onabort fallback text (t.error is null), not an actual capacity problem. Two different local-storage subsystems (blob reads AND IndexedDB writes) failing together, consistently, device-wide, is a stronger signal than the earlier framing ('candidate WebKit blob-durability bug, unconfirmed') allowed for — this now reads more like degraded or corrupted browser storage FOR THIS ORIGIN on this one physical device than a narrow reArmForSave-class miss. The app is already surfacing the strongest signal it has: bookFail/storage_nowrite (mobile/index.html) should be showing this phone's readiness card the 'could not save on this phone' verdict routed to Recovery Inventory, since bookFail now holds all five round IDs. Nothing further is fixable from the JS side; the phone itself likely needs its site data cleared and the PWA reinstalled (Recovery Inventory first, to salvage anything readable) — an operational step, not a code one.",
      "status": "watching"
    }
  ],
  "resolved": [
    {
      "trace": "D1ZMK6_2026-09-20T02-27-12.json (build 410, five builds behind the fleet at the time)",
      "found": "TK109/DZ006/DZ007/DZ008's 2026-09-20 rounds — the exact four the dashboard's Attachment status panel flagged. The phone's own sync log claimed only 1-3 photographs per round were unreadable and 'everything else is sent'; the live bucket held zero photographs for any of the four (checked directly — only each round's JSON sidecar landed). Root cause: putAll() splits a round into concurrent chunks past BATCH_MAX_FILES, and its lane() aggregation kept only the FIRST chunk's unreadable-photo error (`failed=failed||e`), silently dropping every other chunk's names even though each one's own batch-unreadable line reached the sync log. TK109's true count was 11 of 12 unreadable; the sentence said 3.",
      "fix": "Build 416, commit 6dc4c75 — putAll() now collects every chunk's (and every fallback path's) raw unreadable list and builds one combined, accurate summary once all lanes finish. tests/upload-chunk-merge.cjs reproduces the shape and is confirmed non-vacuous against the pre-fix code.",
      "note": "The upload bookkeeping itself (`sent`) was never wrong — nothing was falsely marked delivered, and these four rounds will retry and resolve automatically once this phone updates. This was purely a false-reassurance defect in what the phone told the inspector.",
      "confirmed": "D1ZMK6 updated itself to build 416 within the hour (no manual action taken) and its 2026-09-20T02:57 and T03:29 attempts both correctly report TK109 as 11 of 12 unreadable — the true count, not 3. The fix works in the field, not only in the test."
    }
  ]
}
