{
  "_purpose": "Bookmark for the hourly diagnostic-trace review Routine. Holds the `updated` timestamp (ms epoch, from the _meta/diag/ folder listing on the live backend) of the newest trace file already reviewed. Each hourly check reads this, looks only at traces newer than it, analyzes anything actionable, and advances it — so the review never re-reads the same trace twice and never silently skips one. Updated by that Routine, not by hand.",
  "lastReviewedUpdated": 1789939124374,
  "lastReviewedName": "DQCOSE_2026-09-20T21-18-44.json",
  "lastCheckedAt": "2026-09-20T21:41:00Z",
  "watching": [
    {
      "note": "FIVE occurrences now, five different devices — and the fifth changes what this is. DKB8FF 2026-09-19T22:32 (Id:2), D4W4FT 2026-09-20T02:09 (Id:1), DO3JLV 2026-09-20T03:03 (Id:3), D8B32O 2026-09-20T03:16 (Id:3), and now DQCOSE 2026-09-20T21:18:44 (Id:3, 42 occurrences in ~8s, build 424) — same shape every time, an unhandled-rejection burst of identical 'Object Not Found Matching Id:N, MethodName:update, ParamCount:4' sandwiched between two clean runs (pending:0, run-end err:'' both before and after). No data-loss evidence in any of the five. DQCOSE's platform.ua is the first non-WebKit occurrence: 'Windows NT 10.0; Win64; x64 ... Chrome/141.0.7390.0' — Win32, 16 cores, 32GB, almost certainly a desktop/dev session rather than a field handset. The earlier 'WebKit-native message-bridge artifact' framing was therefore too narrow: this now reproduces on Blink too, which points AWAY from a WebKit-specific native bridge and toward something more generic still outside this app's own JS — a browser extension or DevTools-protocol messaging artifact hitting a torn-down port, which is consistent with there being no 'update' RPC anywhere in this app's own code on any platform. Still not fixable from here on that reading. Remaining at 'known benign, recurring' — still logging each new occurrence in case one ever arrives WITHOUT a clean run after it, which would change this.",
      "status": "known-benign-recurring"
    },
    {
      "note": "REVISED after the maintainer confirmed reinstall does NOT fix this, on more than one phone — the 'corrupted storage on this one device' framing below was wrong, and a second, independent device (DZZ7H3) confirmed it: as of 2026-09-19T03:25 DZZ7H3 (five builds behind the fleet at 403, and silent since — no trace at all for 24h+, likely also failing to reach the update path) showed the IDENTICAL shape on every queued round (TK127/TK155/TK122/DZ004): every remaining photo NotFoundError from all three readers, AND writeback-fail ('save failed — storage may be full', t.error null) on every single record on every attempt — the exact same dbPut(fresh) bookkeeping write D1ZMK6 has been failing on since 2026-09-20T01:27. Two independently-reinstalled devices hitting the identical CODE-LEVEL symptom on the identical write (mobile/index.html's syncNow() write-back, the one that records upTo/up/sent) is strong evidence this is not per-device storage corruption. A full multi-agent audit of the upload/draft/manifest pipeline (10 independent read-only investigations, cross-checked) converged on real, confirmed, previously-undocumented gaps — none proving the deep platform mechanism, all independently worth shipping: (1) attNote's sha256Of used only Blob.arrayBuffer(), so a photo failing that one reader got a manifest entry permanently stuck 'dirty', making buildPackage() (Share/Export ZIP) re-race its own unguarded, non-revision-checked dbPut(rec) on every future Share/Export press — matching the maintainer's own stated fact ('we are already doing share and zip, for a long time'); (2) that buildPackage() write had no revision check at all, unlike syncNow's own two write-backs, so it could silently revert upTo/sent/receipt fields a concurrent sync attempt had just written; (3) filesForRecord() recomputes a deterministic filename on every retry and its caller re-persisted the record's entire Blob/File payload regardless, even when nothing had changed; (4) dbPut/dbDel only listened at the transaction level, so a real per-request DOMException (QuotaExceededError or whatever this actually is) was lost behind the generic 'storage may be full' guess whenever t.error came back null, which is exactly what both D1ZMK6 and DZZ7H3 have been showing. Build 418 fixes all four, ships the revision-checked merge, the fallback-hashing fix, the no-op-write skip, and captures the real per-request error going forward — plus adds telemetry to the one save-time signal (verifySavedRec's verify_fail path) that never reached a trace before, so the next occurrence tells us whether the corruption predates Save or happens after it. The deepest question — why the bookkeeping dbPut fails device-wide, repeatedly, with a null transaction error, at effectively zero quota usage — remains UNCONFIRMED at the platform level; build 418 cannot prove or disprove a WebKit-specific storage defect, it can only reduce how often the suspect write happens and, going forward, capture what the browser actually says instead of a guess.",
      "status": "watching-build-418"
    },
    {
      "note": "FIRST CONFIRMED PLATFORM ON EITHER AFFECTED DEVICE. D1ZMK6_2026-09-20T05-50-43.json (build 417 — build 418 had not reached it yet) is the first trace this phone has ever sent carrying platform.ua, because that field was only added in build 417 (commit c4ee6d9, shipped hours before this trace). It reads: 'Mozilla/5.0 (iPhone; CPU iPhone OS 18_7 like Mac OS X) AppleWebKit/605.1.15 ... Version/26.6.1 Mobile/15E148 Safari/604.1' — an iPhone on iOS 18.7, Safari/WebKit. Every theory this investigation has carried for the underlying platform mechanism (reArmForSave's comment, the build-418 write-up above, every one of the 10 audit findings) explicitly hedged on 'if this is WebKit' — this is the first field data point that actually confirms the affected population is WebKit, not a guess from storage-quota size or the BL007 Android control staying clean. Same shape as every prior trace on this device: DZ007 and DZ008 both failed to read all-but-one photo (NotFoundError, all three readers) AND the bookkeeping writeback failed with the same null-error 'storage may be full' text. bookFail is now 7. No new code action from this trace alone — it corroborates the mechanism build 418 already targets, on a device build 418 has not reached yet. Recording it because it's the single most concrete confirmation this investigation has had.",
      "status": "confirmed-webkit-ios18.7"
    },
    {
      "note": "BUILD 418 REACHED D1ZMK6 WITHIN THE HOUR, AND ITS OWN REQUEST-LEVEL ERROR CAPTURE CAME BACK NULL TOO — a real, informative negative result, not a non-event. Three new build-418 traces (06:33-06:35): bookFail dropped from 7 to 0 (build 418's fixes are landing — the manifest/buildPackage/filesForRecord gaps are no longer accumulating unrecorded work), but writeback-fail still fires repeatedly (19, 15, 14 occurrences per run) with the exact same 'save failed — storage may be full' text. Build 418 shipped a request-level rq.onerror listener specifically to catch a real DOMException the transaction-level t.error (nullable by spec) might be losing — and it caught nothing either: the request itself never reports an error. That rules out a request-level failure (constraint violation, data-clone issue) and narrows this to the TRANSACTION COMMIT phase itself failing with no reason exposed to JS at all, which is a materially different and stranger claim than 'sometimes t.error is null' — it says the browser's own commit path is failing silently, repeatedly, for a multi-megabyte-Blob write, at a reported 0% quota. Build 419 adds a `.phase` tag (which event fired: abort vs error) and `.hadReqErr` to every write-back-fail and save-write-fail trace line, so the next occurrence will finally distinguish an abort from an error event — one further, cheap diagnostic step, not a claimed fix. No code fix is possible yet: the honest state is that this project has now exhausted what request/transaction-level instrumentation can tell it without a live WebKit devtools session on the affected phone, which is outside what this investigation can arrange.",
      "status": "watching-build-419"
    },
    {
      "note": "BUILD 420 — TWO MITIGATION ATTEMPTS, EXPLICITLY NOT CLAIMED AS THE FIX, ASKED FOR BEFORE WIDER ROLLOUT (the maintainer does not want 10-20 more users hitting this in the days ahead). (1) dbFailStreak/dbReconnect: dbPut/dbDel now force-close and reopen the IndexedDB connection after three consecutive write failures. Rationale, not proof: the exact same write, on the exact same records, succeeded once at Save time on what was then a freshly-opened connection, then failed 19 times in a row later in the same session on that long-lived connection — a long-lived-connection-degrades pattern is at least consistent with the data, and reopening is cheap and safe (IDBDatabase.close() waits for in-flight work). If this helps, the next field trace should show writeback-fail runs capped near 3 occurrences per streak instead of climbing into the teens, and a 'db-reconnect' line in the sync log. If it does NOT help — writeback-fail keeps recurring in long runs anyway — that rules out the connection-staleness theory cleanly, which is itself useful. (2) visInfo(): every writeback-fail/save-write-fail line now records document.hidden/visibilityState and ms since the last visibility change, to test whether failures cluster around backgrounding/foregrounding transitions (a documented iOS PWA reliability category, and armRetry's own timers are exactly the kind of thing that queues up while hidden). Neither change has field data yet — watching for the next occurrence on D1ZMK6 or DZZ7H3 to read: does writeback-fail still climb past 3 in one run (reconnect isn't helping, or isn't reachable), and what do vis/hidden say at the moment it fails.",
      "status": "watching-build-420"
    }
  ],
  "resolved": [
    {
      "trace": "D1ZMK6_2026-09-20T02-27-12.json (build 410, five builds behind the fleet at the time)",
      "found": "TK109/DZ006/DZ007/DZ008's 2026-09-20 rounds — the exact four the dashboard's Attachment status panel flagged. The phone's own sync log claimed only 1-3 photographs per round were unreadable and 'everything else is sent'; the live bucket held zero photographs for any of the four (checked directly — only each round's JSON sidecar landed). Root cause: putAll() splits a round into concurrent chunks past BATCH_MAX_FILES, and its lane() aggregation kept only the FIRST chunk's unreadable-photo error (`failed=failed||e`), silently dropping every other chunk's names even though each one's own batch-unreadable line reached the sync log. TK109's true count was 11 of 12 unreadable; the sentence said 3.",
      "fix": "Build 416, commit 6dc4c75 — putAll() now collects every chunk's (and every fallback path's) raw unreadable list and builds one combined, accurate summary once all lanes finish. tests/upload-chunk-merge.cjs reproduces the shape and is confirmed non-vacuous against the pre-fix code.",
      "note": "The upload bookkeeping itself (`sent`) was never wrong — nothing was falsely marked delivered, and these four rounds will retry and resolve automatically once this phone updates. This was purely a false-reassurance defect in what the phone told the inspector.",
      "confirmed": "D1ZMK6 updated itself to build 416 within the hour (no manual action taken) and its 2026-09-20T02:57 and T03:29 attempts both correctly report TK109 as 11 of 12 unreadable — the true count, not 3. The fix works in the field, not only in the test."
    }
  ]
}
